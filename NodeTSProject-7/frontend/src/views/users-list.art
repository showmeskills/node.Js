 {{each data}}
 <tr>
    <td>{{$index + 1}}.</td>
    <td>{{$value.username}}</td>
    <td>
    <button class="btn btn-warning remove" data-id="{{$value._id}}">delete</button>
    </td>
</tr>
{{/each}}